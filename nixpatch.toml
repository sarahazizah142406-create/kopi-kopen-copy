[phases.setup]
nixPkgs = ["...", "nodejs"]

[phases.install]
cmds = ["cd backend && npm install --omit=dev"]

[phases.build]
cmds = ["echo 'Skip build step'"]

[start]
cmd = "cd backend && node src/server.js"
